{
  "name": "ripple-lib-transactionparser",
  "version": "0.6.0",
  "description": "Parses transaction objects to a higher-level view",
  "license": "ISC",
  "scripts": {
    "test": "NODE_ENV=test ./node_modules/.bin/istanbul test ./node_modules/.bin/_mocha -- --reporter spec test/*-test.js",
    "coveralls": "cat ./coverage/lcov.info | ./node_modules/.bin/coveralls",
    "lint": "./node_modules/.bin/eslint --reset -c ./eslint.json ./src/*.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ripple/ripple-lib-extensions.git"
  },
  "main": "src/index.js",
  "keywords": [
    "ripple",
    "ripple-lib",
    "ripple-lib-transactionparser"
  ],
  "engines": {
    "node": ">=0.10.0"
  },
  "dependencies": {
    "bignumber.js": "^2.0.3",
    "lodash": "^3.10.1"
  },
  "devDependencies": {
    "assert-diff": "*",
    "coveralls": "^2.10.0",
    "eslint": "^1.3.0",
    "istanbul": "^0.2.10",
    "mocha": "*"
  }
}
